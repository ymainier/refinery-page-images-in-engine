<ul id="clubs">
  <% @clubs.each do |club| %>
    <li>
      <%= link_to club.name, club_path(club) %>
    </li>
  <% end %>
</ul>

<section id='body_content'>
  <%=raw @page[:body] %>
</section>
<section id='side_body_content'>
  <%=raw @page[:side_body] %>
</section>

